# yaml-lsp

YAML language server plugin for Claude Code.

## Supported Extensions

`.yaml`, `.yml`

## Binary Required

```bash
npm install -g yaml-language-server
```

Already installed in the container image.

## Install Plugin

```
/plugin install /claude-here/plugins/yaml-lsp
```
